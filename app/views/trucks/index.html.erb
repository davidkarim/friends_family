<!-- <p id="notice"><%= notice %></p>

<h1>Listing Trucks</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Web url</th>
      <th>Phone number</th>
      <th>Claimed</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trucks.each do |truck| %>
      <tr>
        <td><%= truck.name %></td>
        <td><%= truck.web_url %></td>
        <td><%= truck.phone_number %></td>
        <td><%= truck.claimed %></td>
        <td><%= link_to 'Show', truck %></td>
        <td><%= link_to 'Edit', edit_truck_path(truck) %></td>
        <td><%= link_to 'Destroy', truck, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Truck', new_truck_path %> -->

<%= week_calendar number_of_weeks: 2 do |date| %>
  <%= date %>
<% end %>
<%= @truck_name %>
<%= week_calendar events: @locations do |date, locations| %>
  <%= date %>

  <% locations.each do |location| %>
    <div>
      <%= location.name %>
    </div>
  <% end %>
<% end %>
